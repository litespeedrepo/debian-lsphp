wget -q -O - https://repo-dev.litespeedtech.com/debian/enable_dev_repo.sh | bash
#if grep -qi 'trixie' /etc/os-release; then
#    echo "deb http://deb.debian.org/debian sid main contrib non-free non-free-firmware" | tee -a /etc/apt/sources.list
    apt update
#fi
